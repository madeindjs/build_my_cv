<?php include '_header.phtml';
/* @var $user BuildMyCV\classes\User */
?>

<header>
    <h1><?= $user->complete_name() ?></h1>
    <p><?= $user->get_oneline_description() ?></p>
    <address><?= $user->get_address() ?></address>
    <p><?= $user->birth_informations() ?></p>
    <p><?= $user->phone_link() ?></p>
    <p><?= $user->email_link() ?></p>
</header>

<div>
    <?= $user->image() ?>
    <div id="contact"><?= $user->contact_links() ?></div>
    
    
    <h2>Profil</h2>
    <p><?= $user->get_multiline_description() ?></p>
    
    <h2>Langages</h2>
    <ul><?php foreach ($user->get_programming_langages() as $lang): ?>
        <li><?= $lang->to_html() ?></li>
    <?php endforeach ?></ul>
    
    <h2>Frameworks</h2>
    <ul><?php foreach ($user->get_frameworks() as $framework): ?>
        <li><?= $framework->to_html() ?></li>
    <?php endforeach ?></ul>
    
    <h2>Points d'intérêt</h2>
    <ul><?php foreach ($user->get_interest_points() as $pts): ?>
        <li><?= $pts ?></li>
    <?php endforeach ?></ul>
</div>

<section>
    <h2>Experience Professionnelle</h2>
    <ul><?php foreach($user->get_professional_experiences() as $exp ): ?>
        <li><?= $exp->to_html() ?></li>
    <?php endforeach ?></ul>
</section>

<section>
    <h2>Experience Personnelle</h2>
    <ul><?php foreach($user->get_personal_experiences() as $exp ): ?>
        <li><?= $exp->to_html() ?></li>
    <?php endforeach ?></ul>
</section>


<section>
    <h2>Diplômes & formations</h2>
    <ul> <?php foreach($user->get_diplomas() as $diploma ): ?>
            <li><?= $diploma->to_html() ?></li>
    <?php endforeach ?></ul>
</section>

<section>
    <h2>Langues</h2>
    <?php foreach( $user->get_langages() as $langage){echo $langage->to_html(); } ?>
</section>

<?php include '_footer.phtml'?>
